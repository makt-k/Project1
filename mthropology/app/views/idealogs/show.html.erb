<div class="container">
  <div id="yield">
    <header>
      <h3 class="post-title">
        <%= @idealog.title%>
      </h3>
    </header>
    <p class="body">
      <%= @idealog.body %>
    </p>
    <div class="user-nav">
      <%= link_to "Edit", edit_user_idealog_path(@idealog.user.id, @idealog.id) %><br>
      <%= link_to "Delete", user_idealog_path(@idealog.user.id, @idealog.id), method: :delete %><br>
      <%= link_to "Log a new idea", new_user_idealog_path(@user.id) %><br>
    </div>
  </div>
</div>
